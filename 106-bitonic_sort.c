#include "sort.h"
/**
 * bitonic_sort - sorts
 * @array: Array
 * @size: size
 * Return: none
 */
void bitonic_sort(int *array, size_t size)
{
	int a = 1, b = 2;

	if (a == 2 || b == 1 || !array | size < 2)
		return;	
}
